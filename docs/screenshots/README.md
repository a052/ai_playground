# Screenshots

Drop screenshots here so they render in the main [README](../../README.md). The README references these filenames:

| File | Suggested capture |
| --- | --- |
| `chat.png` | The main 3-pane view mid-conversation (sidebar + chat + parameter panel). |
| `parameters.png` | The right-hand Parameter Panel with a few enable toggles on. |
| `inspector.png` | The HTTP Inspector modal showing a captured request and the cURL output. |

Tips:

- Use a width around 1280–1600px and the **dark** theme for consistency.
- PNG keeps UI text crisp; keep each file a reasonable size (ideally < 1 MB).
- If you rename or add images, update the links in the README's **Screenshots** section.
